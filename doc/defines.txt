
# defines

vector ::= (dx ,dy)

piece ::= (vectors , angles , dists)
piece_point ::= (Pointer angles)
piece_condition ::= (piece, piece_point, (two_sides(table:T, back:Nil)))

puzzle_point ::= [piece_condiition]
puzzle ::= [puzzle_point]
puzzle_condition ::= (puzzle, [piece_not_used], used_piece_length)

rule_base_A ::= 
